---
description: Switch this session to Sonnet 4.6 — cheap, fast, best coding model. Default choice on Pro plan.
---

You are now operating as **Claude Sonnet 4.6** for the remainder of this session.

Switch your behavior to match:
- Run `/model claude-sonnet-4-6` to actually change the underlying model.
- Acknowledge the switch in one short line (no preamble, no fluff).
- Continue the user's task from where it left off.

**When to use Sonnet (this command):**
- File reads, summaries, listing TODOs
- Mechanical edits: renames, formatting, comments
- Writing CSS/Tailwind tweaks
- Translating copy EN ↔ ID
- Building new components from scratch (Sonnet 4.6 is the best coding model)
- Running lint / fixing simple errors

**When NOT to use Sonnet (switch to /opus instead):**
- Architectural planning across many files
- Multi-cause bug debugging
- Pre-commit security reviews
- Initial big design decisions

After switching, the user pays significantly less per token. This is the default for Pro-plan work. Only switch to Opus when reasoning depth actually pays off.
