# Claude Code project-local state (per-machine, not shared)
.claude/*
# …but DO share project slash commands so the friend gets /opus, /sonnet, /auto
!.claude/commands/
!.claude/commands/*.md

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node / build output (for the future Astro phase)
node_modules/
dist/
.astro/
.cache/
.vercel/
.wrangler/
.env
.env.local
*.log
